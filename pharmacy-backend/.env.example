# === Copy this to .env and fill values ===
PORT=5000

# Put your MongoDB Atlas connection string here (SRV or standard). Example:
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/pharmacy?retryWrites=true&w=majority
MONGO_URI=

# For JWT auth
JWT_SECRET=supersecret_change_me

# Frontend origin for CORS (Vite default localhost:5173). Add production origins separated by commas.
CLIENT_ORIGINS=http://localhost:5173
